/**
 * Sunniesnow, a web rhythm game.
 *
 * The default export is the object that used to be the global variable `Sunniesnow`.
 * This entry point uses the `pixi.js` package as the PixiJS library;
 * import `sunniesnow/node` instead to use `@pixi/node`.
 */
import Sunniesnow from '../js/index.js';

export default Sunniesnow;
