import Sunniesnow from '../Sunniesnow.js';
import PIXI from '../pixi.js';

// Kept empty. Just use instanceof to check of a filter is from chart.
Sunniesnow.FilterFromChart = class FilterFromChart extends PIXI.Filter {
};
