import * as PIXI from '@pixi/node';

// The PixiJS library used by the game when it runs on Node.js.
// This module is used in place of ./pixi.js by the `sunniesnow/node` entry point.
export default PIXI;
