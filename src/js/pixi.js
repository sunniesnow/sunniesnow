import * as PIXI from 'pixi.js';

// The PixiJS library used by the game.
// The `sunniesnow/node` entry point swaps this module for ./pixi-node.js,
// which provides @pixi/node instead.
export default PIXI;
